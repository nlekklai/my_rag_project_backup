from langchain_core.prompts import PromptTemplate

# =================================================================
# 0. PDCA Phase Mapping Helper
# -----------------------------------------------------------------
PDCA_PHASE_MAP = {
    1: "Plan (P)",
    2: "Plan (P) + Do (D)",
    3: "Plan (P) + Do (D) + Check (C)",
    4: "Plan (P) + Do (D) + Check (C) + Act (A)",
    5: "PDCA ครบวงจร + Innovation & Executive Oversight"
}

# =================================================================
# 1. Assessment Prompt (ประเมิน Statement) - [FIXED FOR STRICT JSON ENFORCEMENT]
# -----------------------------------------------------------------
SYSTEM_ASSESSMENT_PROMPT = """คุณคือผู้เชี่ยวชาญด้านการประเมินวุฒิภาวะองค์กรตามกรอบ SE-AM (L3+)
คุณต้องประเมินหลักฐานเพื่อตัดสินสถานะและให้คะแนน โดย **ต้อง** ประเมิน PDCA Breakdown Score ในฟิลด์ P, D, C, A ด้วย (คะแนน 0, 1, หรือ 2)
เหตุผล ('reason') **ต้อง** อ้างอิงและระบุแหล่งที่มา (Citation) จาก Context อย่างชัดเจน (เช่น [SOURCE: filename]) เพื่อสนับสนุนการให้คะแนน PDCA
***สำคัญที่สุด: ผลลัพธ์สุดท้ายต้องเป็น JSON Object ที่ถูกต้องและเป็นไปตาม JSON Schema ที่กำหนดไว้เท่านั้น ห้ามมีข้อความใดๆ ก่อนหรือหลัง JSON เด็ดขาด***
"""

USER_ASSESSMENT_TEMPLATE = """
--- ข้อมูลพื้นฐาน ---
ชื่อเกณฑ์ย่อย: {sub_criteria_name} ({sub_id})
ระดับที่กำลังประเมิน: L{level} (วงจร PDCA: {pdca_phase})

--- มาตรฐานที่ต้องตรวจสอบ (Standard Statement) ---
Level {level}: {statement_text}

--- ข้อจำกัดระดับ (Level Constraint) ---
{level_constraint} 

--- หลักฐานที่พบ (Evidence Context) ---
{context}

--- ตัวอย่าง JSON Output (รวม PDCA) ---
{{
  "score": 0,
  "reason": "หลักฐานไม่ครบองค์ประกอบ Plan ที่จำเป็น เช่น ไม่มีการกำหนดวิสัยทัศน์/ทิศทาง/นโยบาย [SOURCE: KM1.1L306...]",
  "is_passed": false,
  "P_Plan_Score": 0,
  "D_Do_Score": 0,
  "C_Check_Score": 0,
  "A_Act_Score": 0
}}

--- คำสั่ง ---
โปรดประเมินหลักฐานตามเกณฑ์ SE-AM:
1. หลักฐานใน Context เพียงพอต่อการยืนยัน Level {level} (PDCA Phase {pdca_phase}) หรือไม่
2. **ต้องพิจารณาหลักฐานภายใต้ 'ข้อจำกัดระดับ' เท่านั้น**
3. ตอบผลลัพธ์เป็น JSON ตาม SCHEMA เท่านั้น โดย **ต้อง** ใส่ค่า P_Plan_Score, D_Do_Score, C_Check_Score, A_Act_Score
"""


ASSESSMENT_PROMPT = PromptTemplate(
    input_variables=["sub_criteria_name", "sub_id", "level", "pdca_phase", "statement_text", "context", "level_constraint"],
    template=USER_ASSESSMENT_TEMPLATE
)
USER_ASSESSMENT_PROMPT = ASSESSMENT_PROMPT


# =================================================================
# 2. Low-Level Prompt (L1/L2) - [FIXED FOR STRICT JSON ENFORCEMENT]
# -----------------------------------------------------------------
SYSTEM_LOW_LEVEL_PROMPT = """
คุณคือผู้ประเมิน SE-AM สำหรับระดับ L1 (Plan) และ L2 (Do)
คุณต้องประเมินและให้คะแนน PDCA Breakdown Score (P, D, C, A) ตั้งแต่ 0 ถึง 2 (0=ไม่มี, 1=บางส่วน, 2=ครบถ้วน) โดยอิงจากหลักฐานที่พบเท่านั้น
เหตุผล ('reason') **ต้อง** อ้างอิงและระบุแหล่งที่มา (Citation) จาก Context อย่างชัดเจน (เช่น [SOURCE: filename])
***สำคัญที่สุด: ผลลัพธ์สุดท้ายต้องเป็น JSON Object ที่ถูกต้องและเป็นไปตาม JSON Schema ที่กำหนดไว้เท่านั้น ห้ามมีข้อความใดๆ ก่อนหรือหลัง JSON เด็ดขาด***
"""

USER_LOW_LEVEL_PROMPT = """
--- ข้อมูลการประเมิน ---
Sub-Criteria ID: {sub_id}
Sub-Criteria Name: {sub_criteria_name}
Level: {level} (PDCA: {pdca_phase})

--- Statement ---
"{statement_text}"

--- Level Constraint ---
{level_constraint}

--- หลักฐาน ---
{context}

--- การตัดสินใจที่ต้องปฏิบัติตาม ---
1. **🔥 คำสั่ง L2 (Softened Filter):** ถ้า Level ปัจจุบันคือ **L2 (Do - การดำเนินงาน)**
   - คุณ **ต้องระมัดระวัง** และไม่ใช้หลักฐานประเภท **'แผนกลยุทธ์', 'วิสัยทัศน์', 'นโยบาย'** เป็นหลักฐานหลักในการตัดสิน **PASS**
   - การตัดสินใจ **Pass/Fail** ต้องอิงจากหลักฐานที่แสดง **'การดำเนินงาน', 'การขับเคลื่อน', 'การปฏิบัติงาน', หรือ 'รายงานผล'** เท่านั้น
2. **✅ แนวทางการตีความหลักฐาน (Success Examples):** หากหลักฐานไม่ได้ระบุคำตรงตัว **แต่สามารถอนุมานเชิงตรรกะได้อย่างสมเหตุสมผล** ว่าเกณฑ์นั้นถูกบรรลุ ให้ถือว่า **PASS** ตัวอย่างเช่น:
   - "คณะกรรมการ KM" หรือ "ToR คณะทำงาน" หมายถึงมี **ความรับผิดชอบที่ชัดเจน**
   - "มีตัวแทนจากฝ่ายต่างๆ" หมายถึง **การทำงานข้ามสายงาน (cross-functional)**
   - "การแต่งตั้งโดยผู้บริหารระดับสูง" สามารถอนุมานจากเอกสารทางการ/คำสั่งแต่งตั้งได้
3. **ต้องพิจารณาหลักฐานภายใต้ 'ข้อจำกัดระดับ' เท่านั้น**
4. **ผ่าน (Pass):** หากหลักฐานใน Context ยืนยัน Statement ได้อย่างชัดเจนและสอดคล้องกับข้อจำกัด (รวมถึงการอนุมานที่สมเหตุสมผล)
5. **ไม่ผ่าน (Fail):** หากหลักฐานขาดหาย, ไม่เกี่ยวข้อง, หรือไม่ครอบคลุมข้อกำหนดหลักตาม Statement

--- ตัวอย่าง JSON Output (Pass/Fail + PDCA) ---
Pass Example:
{{
  "score": 4,
  "reason": "หลักฐานแสดงถึงการดำเนินงานตามแผนชัดเจน มีรายงานผลครบถ้วน และใช้การอนุมานเชิงตรรกะเพื่อยืนยันโครงสร้างคณะทำงาน [SOURCE: KM5.2L101...]",
  "is_passed": true,
  "P_Plan_Score": 2,
  "D_Do_Score": 2,
  "C_Check_Score": 1,
  "A_Act_Score": 1
}}
Fail Example:
{{
  "score": 1,
  "reason": "หลักฐานไม่เกี่ยวข้องกับการปฏิบัติงานจริง เป็นเพียงเอกสารนโยบาย [SOURCE: KM1.1L101...]",
  "is_passed": false,
  "P_Plan_Score": 1,
  "D_Do_Score": 0,
  "C_Check_Score": 0,
  "A_Act_Score": 0
}}

--- คำสั่ง ---
ประเมินหลักฐานตาม Statement และ Level Constraint แล้วตัดสินใจว่า 'ผ่าน' หรือ 'ไม่ผ่าน' และระบุเหตุผล (reason) พร้อมค่า PDCA Score ทั้งหมดอย่างชัดเจนและกระชับในรูปแบบ JSON Object:
"""


# =================================================================
# 3. Action Plan Prompt
# -----------------------------------------------------------------
SYSTEM_ACTION_PLAN_PROMPT = """
คุณคือผู้เชี่ยวชาญด้านวางแผนกลยุทธ์ (Strategic Planner)
หน้าที่: สร้าง Action Plan เพื่อยกระดับ Maturity Level ของเกณฑ์ที่ Fail
ต้องสอดคล้อง PDCA Phase
"""

ACTION_PLAN_TEMPLATE = """
--- เกณฑ์ที่ล้มเหลว ---
Sub-Criteria ID: {sub_id}
Target Next Level: L{target_level}
Failed Statements:
{failed_statements_list}

--- ตัวอย่าง JSON Output ---
[
  {{
    "Phase": "Plan",
    "Goal": "กำหนดวิสัยทัศน์ KM ให้ชัดเจน",
    "Actions": ["ประชุมผู้บริหารกำหนดวิสัยทัศน์", "สื่อสารแผนกลยุทธ์"],
    "Responsible": "ฝ่ายกลยุทธ์",
    "Key_Metric": "จำนวนแผนที่เผยแพร่",
    "Tools_Templates": "Template แผนกลยุทธ์",
    "Verification_Outcome": "ตรวจสอบเอกสารและความเข้าใจผู้บริหาร"
  }}
]

--- คำสั่ง ---
สร้าง Action Plan ให้สอดคล้อง PDCA และแก้ไข Gap ตาม Failed Statements
ตอบ JSON Array เท่านั้น
"""

ACTION_PLAN_PROMPT = PromptTemplate(
    input_variables=["sub_id", "target_level", "failed_statements_list"],
    template=ACTION_PLAN_TEMPLATE
)

# =================================================================
# 4. Evidence Description Prompt
# -----------------------------------------------------------------
SYSTEM_EVIDENCE_DESCRIPTION_PROMPT = """
คุณคือผู้เชี่ยวชาญด้านวิเคราะห์หลักฐาน (Evidence Analyst)
หน้าที่: สรุปหลักฐานและให้ข้อเสนอแนะสำหรับ Next Level
"""

USER_EVIDENCE_DESCRIPTION_TEMPLATE = """
--- เกณฑ์ ---
{sub_id} Level {level}: {sub_criteria_name}

--- หลักฐาน ---
{context}

--- คำสั่ง ---
สร้าง JSON Object:
- "summary": สรุปหลักฐานสำคัญ
- "suggestion_for_next_level": ข้อเสนอแนะเพื่อผ่าน Level ต่อไป
- ใช้เฉพาะข้อมูลใน Context
"""

EVIDENCE_DESCRIPTION_PROMPT = PromptTemplate(
    input_variables=["sub_criteria_name", "level", "sub_id", "context"],
    template=USER_EVIDENCE_DESCRIPTION_TEMPLATE
)

# =================================================================
# 5. Schema Definition (Simplified)
# -----------------------------------------------------------------
# จำเป็นต้องมีถ้ามีการเรียกใช้ Output Parser
# ตัวอย่าง Schema สำหรับ Assessment (อาจจะอยู่ในไฟล์อื่น)
# ASSESSMENT_RESULT_SCHEMA = {
#     "type": "object",
#     "properties": {
#         "llm_score": {"type": "integer", "description": "0-5"},
#         "reason": {"type": "string"},
#         "is_passed": {"type": "boolean"}
#     },
#     "required": ["llm_score", "reason", "is_passed"]
# }